<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Blog</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #ff9a9e, #fad0c4, #fad0c4);
      background-size: 200% 200%;
      animation: gradientMove 6s ease infinite;
      font-family: "Poppins", sans-serif;
      min-height: 100vh;
      padding-top: 90px;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Header Glass Navbar */
    .glass-nav {
      background: rgba(255, 255, 255, 0.25);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255,255,255,0.5);
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      position: fixed;
      top: 0;
      width: 100%;
      z-index: 1000;
      padding: 15px 0;
    }

    .navbar-brand {
      font-size: 32px;
      font-weight: 800;
      background: linear-gradient(to right, #ff5e62, #ff9966);
      -webkit-background-clip: text;
      color: transparent;
      letter-spacing: 1px;
    }

    .nav-link {
      font-size: 17px;
      color: #4b2e2e !important;
      font-weight: 600;
      transition: .3s;
    }

    .nav-link:hover {
      color:  #7a4a4a  !important;
      transform: translateY(-2px);
    }

    /* Page Title */
    .page-title {
      font-size: 42px;
      font-weight: 800;
      text-align: center;
      margin-bottom: 35px;
      margin-top: 20px;
      background: linear-gradient(to right, #ff5e62, #ff9966);
      -webkit-background-clip: text;
      color: transparent;
      letter-spacing: 1px;
      text-shadow: 0 3px 10px rgba(255, 100, 100, 0.3);
    }

    /* Blog Form Card */
    .blog-card {
      background: rgba(255, 255, 255, 0.2);
      border-radius: 22px;
      padding: 45px;
      backdrop-filter: blur(15px);
      border: 1px solid rgba(255,255,255,0.4);
      box-shadow: 0 20px 40px rgba(0,0,0,0.15);
      max-width: 750px;
      margin: auto;
      animation: fadeInUp 0.9s ease;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Form Style */
    label {
      font-weight: 600;
      color: #4b2e2e;
      margin-bottom: 5px;
    }

    .form-control {
      border-radius: 14px;
      padding: 12px;
      background: rgba(255,255,255,0.85);
      border: none;
    }

    .form-control:focus {
      transform: scale(1.02);
      box-shadow: 0 0 12px rgba(255, 110, 110, 0.6);
    }

    /* Buttons */
    .btn-primary {
      background: linear-gradient(to right, #ff512f, #dd2476);
      border: none;
      padding: 12px 24px;
      border-radius: 14px;
      font-weight: 600;
      color: #4b2e2e;
      box-shadow: 0 6px 18px rgba(0,0,0,0.25);
      transition: 0.3s;
    }

    .btn-primary:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 28px rgba(0,0,0,0.35);
    }

    .btn-secondary {
      background: rgba(255,255,255,0.4);
      border: 1px solid rgba(255,255,255,0.7);
      padding: 12px 24px;
      border-radius: 14px;
      color: #4b2e2e;
      font-weight: 600;
    }

    .btn-secondary:hover {
      background: rgba(255,255,255,0.7);
      color: #000;
      transform: translateY(-4px);
    }

    /* Footer */
    .simple-footer {
    margin-top: 80px;
    padding: 18px 0;
    text-align: center;
    color: #4b2e2e;
    background: rgba(255, 255, 255, 0.18);
    backdrop-filter: blur(10px);
    border-top: 1px solid rgba(255,255,255,0.35);
  }

  .simple-footer p {
    margin: 0;
    font-size: 15px;
    font-weight: 500;
  }

  .simple-footer .footer-links {
    margin-top: 8px;
  }

  .simple-footer .footer-links a {
    margin: 0 10px;
    font-size: 14px;
    color: #4b2e2e;
    text-decoration: none;
    opacity: 0.9;
    transition: 0.2s;
  }

  .simple-footer .footer-links a:hover {
    opacity: 1;
    text-decoration: underline;
  }
  </style>
</head>

<body>

  <!-- HEADER -->
  <nav class="navbar navbar-expand-lg glass-nav px-4 py-2">
    <a class="navbar-brand" href="/blog/get">MyBlog</a>

    <button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav gap-3">
        <li class="nav-item">
          <a class="nav-link" href="/blog/get">All Blogs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/blog/add">Add Blog</a>
        </li>
      </ul>
    </div>
  </nav>

  <!-- PAGE CONTENT -->
  <div class="container">

    <h2 class="page-title">Create New Blog</h2>

    <div class="blog-card">

      <form action="/blog/add" method="POST" enctype="multipart/form-data">

        <div class="mb-3">
          <label>Blog Title</label>
          <input type="text" name="title" class="form-control" placeholder="Enter blog title" required />
        </div>

        <div class="mb-3">
          <label>Author Name</label>
          <input type="text" name="author" class="form-control" placeholder="Enter author name" required />
        </div>

        <div class="mb-3">
          <label>Content</label>
          <textarea name="content" class="form-control" rows="5" placeholder="Write your blog content..." required></textarea>
        </div>

        <div class="mb-3">
          <label>Upload Blog Image</label>
          <input type="file" name="image" class="form-control" />
        </div>

        <div class="d-flex gap-3">
          <button class="btn btn-primary">Publish Blog</button>
          <a href="/blog/get" class="btn btn-secondary">Cancel</a>
        </div>

      </form>
    </div>

  </div>

  <!-- FOOTER -->
  <footer class="simple-footer">
  <p>© 2025 MyBlog • All Rights Reserved</p>

  <div class="footer-links">
    <a href="/blog/get">Blogs</a>
    <a href="/blog/add">Add Blog</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
  </div>
</footer>

</body>
</html>
